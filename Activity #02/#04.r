# Define the vector v
v <- c(50, 23, 75, 101, 85)

# Find the maximum value in the vector
max_value <- max(v)

# Check if the maximum value is greater than 100 and print the appropriate message
if (max_value > 100) {
  cat("Large Maximum\n")
} else {
  cat("Small Maximum\n")
}
