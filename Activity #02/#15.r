# Define the vector
a <- c(6, 7, 9, 10, 12, 14, 50, -4, -3, 60)

# Find the minimum and maximum values
min_value <- min(a)
max_value <- max(a)

cat("Min value:", min_value, "\n")
cat("Max value:", max_value, "\n")
